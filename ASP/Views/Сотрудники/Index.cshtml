@model IEnumerable<ASP.Models.Сотрудники>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ФИО)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Стаж)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ФИО)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Стаж)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.КодСотрудника }) |
            @Html.ActionLink("Details", "Details", new { id=item.КодСотрудника }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.КодСотрудника })
        </td>
    </tr>
}

</table>
